import React from "react";
import { ClassicApp } from "../app/ClassicApp";

/** Every device opens the same source, conversation, and sensing workspace. */
export function RootNavigator() {
  return <ClassicApp />;
}
