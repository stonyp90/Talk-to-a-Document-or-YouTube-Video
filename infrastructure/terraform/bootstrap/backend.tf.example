# After the first operator bootstrap, copy to backend_override.tf and migrate
# the LOCAL bootstrap state with init -migrate-state. Never use the demo key.
terraform {
  backend "s3" {
    key          = "bootstrap/terraform.tfstate"
    encrypt      = true
    use_lockfile = true
  }
}
